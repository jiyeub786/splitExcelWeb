# splitExcelWeb 개선 아이디어 & 반영 내역

`plan.md`에서 요청한 개선 아이디어 정리와, 이번에 바로 반영 가능했던 항목의 구현 내역입니다.

## 0. 실제 파일로 브라우저 테스트한 결과 (발견 & 수정)

`storage/uploads/15e88dee2923485e995afb4591c3fbd6/source_원본파일.xlsx`
(60MB, 시트당 최대 122,648행 × 20열의 실제 데이터)를 기준으로 로컬 서버를 띄우고 Chrome으로
직접 조작하며 테스트했고, 이 과정에서 아래 3가지 문제를 발견해 바로 고쳤습니다.

1. **[버그] 시트 작업 정의 표가 깨짐** — "범위 선택" 버튼을 추가하면서 `<td class="range-cell">`에
   `display: flex`를 직접 줬는데, 브라우저가 그 셀을 테이블 레이아웃에서 이탈시켜 "복사 범위"/
   "붙여넣기 위치" 두 칸이 표 밖으로 튀어나와 다음 줄에 겹쳐 보이는 현상이 있었습니다. `<td>` 안에
   `<div class="range-cell">`을 한 겹 더 두고 그 div에 flex를 적용하도록 고쳤습니다
   (`static/app.js`).
2. **[정확성] 분리 대상 자동 추출이 실제 파일의 다중 행 헤더를 못 다룸** — 실제 파일은 1~3행이
   제목/부제/컬럼헤더로 된 3행짜리 헤더 구조였는데, "1행만 헤더로 제외" 체크박스로는 3행째의
   헤더 텍스트("구분")가 고유값으로 잘못 섞여 들어갔습니다. 체크박스를 "제외할 헤더 행 수" 숫자
   입력으로 바꿔 임의의 헤더 행 수를 지정할 수 있게 했습니다(`app/preview.py`,
   `app/main.py`, `static/index.html`, `static/app.js`).
3. **[성능] 대용량 파일에서 미리보기/추출이 느림** — openpyxl은 이 60MB 파일을 `read_only=True`로
   여는 것만으로도 5.5초가 걸렸고(행을 하나도 안 읽은 상태), 그리드 미리보기는 약 6초, 열 값
   추출은 약 11초가 걸렸습니다. 파서를 `python-calamine`(Rust 기반)으로 교체해 같은 파일 기준
   워크북 열기 0.4초, 그리드 미리보기 약 3초, 열 값 추출 약 3.5초로 줄였습니다(요구사항이 바뀐 건
   아니라 순수 라이브러리 교체이며 `requirements.txt`도 갱신했습니다). 그래도 수십만 행 파일은
   열 때마다 몇 초는 걸리므로, 자주 여닫는 화면이라면 다음 단계로 "백로그"의 캐싱을 고려할 것.

수정 후 위 파일로 시트명 자동완성, 그리드 미리보기(원본/양식 각각), 클릭-드래그 범위 선택,
열 자동 추출, "양식파일=원본파일과 동일" 체크박스를 모두 재확인했고 브라우저 콘솔 에러 없이
정상 동작했습니다.

> **실제 Excel 분리 작업(Job) 실행 자체는 이번 테스트에서 돌려보지 않았습니다.** 이 세션의
> 자동화 브라우저/셸은 사용자의 인터랙티브 데스크톱 세션이 아닐 수 있어(CLAUDE.md에도 명시된
> 기존 제약), `win32com.client.Dispatch("Excel.Application")` 호출이 응답 없이 멈출 위험이 있고
> 그러면 서버의 단일 워커 스레드 전체가 막혀버립니다. `excel_processor.py`/`job_manager.py`는
> 이번에 건드리지 않은 원본 로직 그대로이므로, 실제 "작업 시작"/"테스트 시작" 버튼 클릭까지는
> 사용자 본인 데스크톱 세션에서 한 번 직접 확인해보시길 권장합니다.

## 0-2. 실제로 작업을 돌려보고 알려주신 문제 & 수정

사용자가 직접 실제 작업 흐름을 써보고 알려준 4가지 문제를 모두 반영했습니다.

1. **[버그] 범위 선택 드래그 중 스크롤이 안 따라감** — 시트 열이 많아 미리보기가 스크롤되는
   상황에서, 드래그로 화면 밖 셀까지 선택하려 해도 그리드가 자동으로 스크롤되지 않아 끝까지
   선택할 수 없었습니다. 두 가지를 함께 추가했습니다.
   - 드래그 중 마우스가 그리드 가장자리(위/아래/좌/우 36px 이내)에 닿으면 해당 방향으로 자동
     스크롤하고, 스크롤되며 커서 아래로 들어온 셀을 실시간으로 선택 범위에 반영합니다.
   - 드래그 없이도 먼 셀을 바로 지정할 수 있도록, 첫 셀 클릭 후 끝 셀을 **Shift+클릭**하면 그
     사이 전체가 범위로 잡히는 엑셀과 동일한 방식도 지원합니다(`static/app.js`).
2. **[버그] 붙여넣기 위치 선택 시 헤더 아래 빈 행을 클릭할 수 없음** — 헤더가 3행까지인 양식
   파일에서 4행(첫 데이터 행)에 붙여넣어야 하는데, 미리보기 그리드가 실제 데이터가 있는 범위
   (이 경우 헤더 3행)까지만 렌더링되어 그 아래 빈 셀은 화면에 아예 나타나지 않아 클릭할 수
   없었습니다. 그리드 응답이 항상 요청한 `max_rows`(200) × `max_cols`(40)만큼 빈 셀로 채워진
   상태로 오도록 고쳤습니다(`app/preview.py`) — 이제 데이터가 없는 빈 양식파일이라도 원하는
   위치를 클릭으로 지정할 수 있습니다.
3. **[UI] 글자가 잘려 보이는 문제** — 실제로는 "범위 선택" 버튼을 넣으며 좁아진 시트명 칸이
   원인이었습니다(`①에듀파인계약정보` 같은 긴 시트명이 `①에듀파인계` 정도까지만 보였음).
   시트작업 정의 표에 열 너비를 고정 비율로 지정(`<colgroup>`)해 시트명 칸이 항상 충분한 폭을
   갖도록 하고, 그래도 넘치면 말줄임(`...`) 처리 + 마우스오버 시 전체 텍스트 툴팁이 뜨도록
   했습니다. 화면 자체도 더 넓게 썼습니다(본문 최대 너비 900px → 1240px, 범위 선택 모달도
   900px → 1160px)(`static/index.html`, `static/style.css`, `static/app.js`).
   > 이 항목은 "글자가 씹히거나..."로 짧게 언급하셔서 가장 유력한 원인으로 추정해 고쳤습니다.
   > 다른 화면에서도 비슷한 문제가 남아있다면 스크린샷이나 위치를 알려주시면 바로 잡겠습니다.
4. **[개선] 저장 명칭/저장 일자 기본값 자동 채움** — 원본파일을 선택하면 "저장 명칭"이 비어있을
   때 파일명(확장자 제외)으로, "저장 일자"가 비어있을 때 오늘 날짜(YYYYMMDD)로 자동 채워지도록
   했습니다. 이미 값을 입력해둔 경우는 덮어쓰지 않습니다(`static/app.js`).
5. **[개선] 복사 범위를 "마지막 행까지" 한 번에 확장** — 위 1번 자동 스크롤 드래그를 넣고 나서도,
   12만 행이 넘는 실제 시트에서는 자동 스크롤로 끝까지 드래그하는 것 자체가 비현실적이라는
   후속 피드백을 받아 추가했습니다. 시작 셀(과 필요하면 열 범위)만 클릭/드래그로 지정한 뒤
   모달의 **"마지막 행까지 확장"** 버튼을 누르면, 지정해둔 열 범위는 그대로 유지한 채 끝 행만
   시트의 실제 마지막 데이터 행으로 자동 계산해서 채웁니다(그리드 미리보기 응답에 이미 포함된
   실제 시트 크기 정보를 사용, 서버 재조회 없음)(`static/index.html`, `static/app.js`).
   예: A4 클릭 후 D4까지 Shift+클릭으로 열 범위(A:D)만 잡고 "마지막 행까지 확장"을 누르면
   122,648행짜리 실제 시트에서 `A4:D122648`이 즉시 채워지는 것을 확인했습니다. 붙여넣기 위치
   선택(단일 셀)에는 해당 없어 그 모드에서는 버튼이 보이지 않습니다.
6. **[개선] "필터 열 번호"를 A/B/C 같은 열 문자로 입력** — 원래 숫자 입력(`1`, `6` ...)이라
   실제 시트를 보면서 "F열이 몇 번째 열이지?"를 세어야 했는데, 복사 범위/붙여넣기 위치와 같은
   방식으로 열 문자(`A`, `F` ...)를 직접 입력하도록 바꿨습니다. 내부적으로 저장/전송되는 값은
   기존과 동일한 1부터 시작하는 숫자(`filter_index`)라 백엔드(`app/models.py`,
   `app/excel_processor.py`)는 전혀 건드리지 않았고, 프론트엔드에서만 문자↔숫자를 변환합니다
   (`static/app.js`, `static/index.html`). 숫자를 그대로 입력해도 동작하도록 하위 호환도
   남겨뒀습니다(대소문자 구분 없이 `A`/`a` 모두 인식, `6`도 그대로 인식). 빈 값이나 잘못된
   입력은 작업 시작 시 알림으로 걸러냅니다.
7. **[개선] 화면 압축 + 타이틀/설명서 추가** — 다음 요청을 한 번에 반영했습니다.
   - **버튼/용어 압축**: "범위 선택" → "선택", "삭제" → 작은 "−" 아이콘 버튼, "테스트 시작
     (1건만)" → "테스트(1건)", "강제 종료(Excel 프로세스 종료)" → "강제 종료", "설정
     다운로드(JSON)"/"설정 불러오기(JSON)" → "다운로드"/"불러오기". 잘려나간 설명은 각 버튼의
     `title` 속성(마우스오버 툴팁)으로 남겨뒀습니다.
   - **전반적인 여백 축소**: 카드 padding, 섹션 간격, 입력창/버튼 높이, 표 셀 padding을 전체적으로
     줄여 화면을 더 조밀하게 썼습니다(`static/style.css`).
   - **분리 대상 목록 여백 문제**: 고정 6줄 높이였던 텍스트영역을 실제 줄 수에 맞춰 자동으로
     늘어나고 줄어들게 바꿨습니다(최소 3줄, 최대 220px, 그 이상은 내부 스크롤). 이 과정에서
     줄번호 거터와 flex로 묶여 있던 게 서로 높이를 밀어주는 순환 참조 버그(`align-items:
     stretch` 기본값 때문에 줄어들지 않고 마지막으로 늘어난 높이에 고정되던 문제)를 발견해
     함께 고쳤습니다(`static/app.js`, `static/style.css`).
   - **타이틀 변경**: "SplitExcel Web" → "엑셀쪼개기(웹버전)"(브라우저 탭 제목과 페이지 상단
     모두)(`static/index.html`).
   - **설명서 버튼 추가**: 헤더 오른쪽에 "설명서" 버튼을 추가, 클릭하면 처음 쓰는 사람도 따라할
     수 있도록 파일 올리기부터 결과 다운로드까지 단계별로 설명하는 모달이 뜹니다(`static/
     index.html`의 `#helpModal`, `static/app.js`, `static/style.css`).

추가로 함께 반영한 것:
- **분리 대상 목록에 줄번호 표시**: 텍스트영역 왼쪽에 줄번호 거터를 붙이고, 아래에 "N개"로 현재
  유효 항목 수를 표시합니다(`static/index.html`, `static/style.css`, `static/app.js`).
- **분리 대상 목록 예시를 실제 사용 사례에 맞게 변경**: "101동/102동/103동" 같은 동/호수 예시
  대신 "서울/경기/부산" 같은 시도·지역 예시로 바꿨습니다(`static/index.html`).

모두 같은 실제 파일(`storage/uploads/15e88dee2923485e995afb4591c3fbd6/`)로 재검증했습니다:
헤더 3행짜리 양식파일에서 4행 이후 빈 칸 클릭 선택, 드래그 중 자동 스크롤(스크롤 0→660px까지
확인), Shift+클릭으로 원거리 범위 지정, 저장 명칭/일자 자동 채움, 줄번호/개수 표시까지 모두
정상 동작을 확인했습니다.

## 0-3. 다크/라이트 테마 전환 + 설명서 보완

1. **다크/라이트 테마 토글**: 헤더에 🌙/☀️ 버튼을 추가해 다크(기본)와 라이트 테마를 전환할 수
   있게 했습니다. 선택값은 `localStorage`에 저장되어 새로고침해도 유지되고, `<head>`의 인라인
   스크립트가 CSS 적용 전에 미리 테마를 지정해 깜빡임(FOUC)이 없습니다. 저장소 루트에 라이트
   테마 토큰 문서 `design2.md`를 새로 만들었습니다 — `design.md`(다크, Binance 분석)와 브랜드
   액센트/타이포그래피/반경은 공유하고 캔버스·표면·헤어라인·텍스트 톤만 라이트용으로 다시
   정의합니다(design.md가 이미 문서화한 Binance의 "라이트 모드(거래성)" 토큰을 그대로 재사용).
   `CLAUDE.md`에도 두 문서의 관계를 한 줄 추가해뒀습니다.
   구현 과정에서 CSS 변수 중 이름에 "dark"가 박혀 있던 것들(`--canvas-dark`,
   `--surface-card-dark`, `--surface-elevated-dark`, `--hairline-dark`, `--on-dark`)을
   테마 중립적인 이름(`--canvas`, `--surface-card`, `--surface-elevated`, `--hairline`,
   `--text-strong`)으로 정리했습니다 — 라이트 테마에서 값이 바뀌는데 이름은 여전히 "-dark"인
   채로 남으면 나중에 유지보수할 때 헷갈리기 때문입니다. 다크/라이트 전 화면을 브라우저로 직접
   전환해가며 확인했고, 콘솔 에러 없었습니다.
2. **설명서 내용 보완**: 데스크톱 버전의 사용법 문서(`splitExcel/UsageUi.ui`, `사용법` 다이얼로그
   원문)를 참고해서 웹 버전 설명서에 없던 내용을 추가했습니다.
   - **⚠ 시작 전 주의사항** 섹션 신설: 작업 전 Excel을 모두 닫아둘 것, Excel 자동화 제어가
     완벽하지 않다는 점, 수식 복사/붙여넣기는 결과를 꼭 검증할 것(`INDEX`/`OFFSET`의 암시적
     교차 위험) 등.
   - 양식파일 설명 보강: "원본에서 값만 지우고(수식은 남기고) 저장한 파일"이라는 원본 문서의
     비유를 추가.
   - 복사 범위/붙여넣기 위치에 구체 예시(`A2:D500` → `A2`) 추가.
   - 옵션 섹션에 줌 레벨 0의 의미("건드리지 않음"), 수식 경로 참조 제거 옵션이 존재하는 이유와
     성능 트레이드오프를 원본 문서 수준으로 보강.
   - 강제 종료 설명에 "이 웹 버전에서는 취소+Excel 강제종료가 버튼 한 번에 처리된다"(데스크톱은
     2번 클릭 필요)는 차이점과 "서버 자체가 꺼지는 게 아니다"라는 오해 방지 문구 추가.

## 1. 이번에 반영한 것

### 1-1. 파일 드래그 앤 드롭 업로드
원본파일/양식파일 `<input type="file">` 영역을 드롭존으로 확장. 클릭해서 선택하는 기존 방식은
유지하면서, 탐색기에서 파일을 끌어다 놓아도 동작하도록 함.

### 1-2. "양식파일 = 원본파일과 동일" 체크박스
원본파일 하나로 원본/양식을 겸용하고 싶은 흔한 케이스(같은 서식의 파일에서 필터링만 다르게
하고 싶을 때)를 위해 체크박스 추가. 체크 시 양식파일 업로드 필드를 숨기고, 제출 시 원본파일을
양식파일 자리에도 그대로 사용함.

> **구조적 한계로 완전히 없애지는 못함**: 데스크톱 버전 설계상 "양식파일"은 붙여넣기 대상이 될
> 별도의 서식/수식 구조를 가진 파일인 경우가 많다(원본은 데이터 원장, 양식은 최종 산출 포맷).
> 두 파일이 다른 게 일반적인 사용 패턴이므로, 완전 자동화는 어렵고 "같은 파일 재사용"이라는
> 흔한 특수 케이스만 우선 지원함.

### 1-3. 시트명 자동완성 (오타 방지)
원본파일을 선택하면 서버에 시트 목록을 물어봐(`POST /api/preview/sheets`, python-calamine 기반이라
Excel COM 없이 즉시 응답) `<datalist>`로 제공. 시트작업 정의 테이블의 "시트명" 입력이 자동완성을
지원해 오타로 인한 실패(원본/양식에 동일한 시트명이 없어서 실패하는 케이스)를 줄임.

### 1-4. 범위 선택 뷰어 (요청하신 "뷰어" 기능)
시트작업 정의 각 행에 **"범위 선택"** 버튼을 추가. 누르면:
- 복사 범위(`copy_range`) 선택: 원본파일의 해당 시트를 읽기 전용 그리드로 미리보기(최대
  200행 × 40열, `POST /api/preview/grid`)하고, 셀을 클릭-드래그하면 `A1:D500` 같은 범위 문자열이
  자동으로 만들어짐. "적용"을 누르면 그 행의 복사 범위 입력에 채워짐.
- 붙여넣기 위치(`paste_range`) 선택: 양식파일의 해당 시트를 같은 방식으로 미리보고, 셀 하나를
  클릭하면 `A2` 같은 위치가 채워짐.

전체 스프레드시트 편집기(Handsontable 등)를 붙이는 대신, 읽기 전용 미리보기 + 클릭 선택으로
범위를 "보면서" 지정할 수 있게 하는 가벼운 방식을 택함 (외부 CDN 의존성/유지보수 비용을 피하기
위함 — 아래 "보류한 것" 참고).

### 1-5. 분리 대상 목록 자동 추출
"분리 대상 목록" 섹션에 열 지정 UI를 추가. 원본파일 + 시트명 + 열(문자 `C` 또는 숫자 `3`) +
제외할 헤더 행 수를 지정하고 "추출"을 누르면 `POST /api/preview/column-values`가 해당 열의
고유값을 등장 순서대로 뽑아 목록 텍스트영역에 채움. 수작업으로 대상 목록을 타이핑/복붙하던
불편을 없앰. (처음엔 "1행만 헤더로 제외" 체크박스였는데, 실제 파일로 테스트해보니 제목/부제가
따로 있는 다중 행 헤더가 흔해서 임의 행 수를 지정하는 숫자 입력으로 바꿈 — 0번 항목 참고.)

### 1-6. 진행률 표시줄
로그 스트림(SSE)에서 `"{대상값} 완료. 처리 시간: N초"` 패턴을 프론트에서 파싱해 완료된 대상 수를
세고, 전체 대상 수(테스트 모드면 1, 아니면 분리 대상 목록 길이) 대비 진행률 바를 표시. 텍스트
로그만 보고 얼마나 남았는지 가늠하던 불편을 줄임.

## 2. 이번엔 보류한 것 (구조적 한계 또는 비용 대비 가치 낮음)

- **원본/양식 파일 완전 통합**: 위 1-2 참고. 일반적인 경우엔 두 파일의 역할이 다르므로 완전히
  없앨 수는 없음.
- **본격적인 인라인 스프레드시트 편집기 도입**(Handsontable, x-spreadsheet 등): 읽기 전용
  미리보기+클릭 선택으로 이미 "보면서 범위 지정"이라는 핵심 가치는 제공됨. 편집 기능까지
  필요해지면(예: 뷰어에서 직접 셀 값 수정) 그때 별도 라이브러리 도입을 검토하는 게 낫다고 판단.
- **대용량 시트(수만 행) 전체 미리보기**: 현재는 최대 200행만 미리보기(성능/응답속도 이유).
  범위 지정은 보통 앞부분 구조만 보면 되므로 실용적으로는 충분하다고 판단하되, 실제 사용해보고
  부족하면 페이지네이션/무한스크롤을 추가할 것.

## 3. 백로그 구현 (2026-08-31 추가 반영분)

이전 "3. 추가 백로그 아이디어"에 적어뒀던 13개 항목 중 12개를 구현했습니다. 남은 1개(수식 위험
함수 경고)는 라이브러리 한계로 이번엔 보류하고 아래 "4. 남은 백로그"에 사유와 함께 남겨뒀습니다.

- **Job 히스토리**(`app/job_manager.py`의 `list_jobs()`, `GET /api/jobs`,
  `static/index.html`의 "9. Job 히스토리" 섹션): 서버가 살아있는 동안 실행한 Job을 최신순으로
  보여줍니다. 상태 배지(대기/실행 중/완료/오류/취소됨), 완료된 Job은 zip 다운로드 + "파일별
  보기"(개별 xlsx 다운로드)를 제공합니다. 메모리 기반이라 서버를 재시작하면 초기화됩니다(디스크
  스캔 기반으로 바꾸면 재시작에도 남길 수 있음 — 아직은 안 함).
- **저장소 정리**(`job_manager._cleanup_old_jobs()`): 서버 시작 시 1회, `storage/uploads/`,
  `storage/results/` 중 7일(`JOB_RETENTION_DAYS`)보다 오래된 Job 디렉터리/zip을 자동 삭제합니다.
  서버가 막 시작된 시점이라 `self.jobs`가 비어있어 "지금 실행 중인 Job"과 충돌할 위험이 없습니다.
- **분리 대상 사전 검증**(`preview.validate_split_list()`, `POST /api/preview/validate-split-list`,
  UI의 "검증" 버튼): 분리 대상 목록의 각 값이 실제로 그 열에 존재하는지 확인해서, 없는 값을
  알림으로 보여줍니다(오타로 빈 결과 파일이 생기는 것 방지).
- **설정 자동 저장**(`static/app.js`의 `saveDraft`/`restoreDraft`): 입력값이 바뀔 때마다(800ms
  디바운스) `localStorage`에 저장하고, 페이지를 다시 열면 자동으로 복원합니다. 파일 자체는
  저장할 수 없어(브라우저 보안 제약) 원본/양식 파일은 매번 다시 선택해야 합니다.
- **다중 대기열 상태 표시**(`job_manager.get_queue_position()`, `GET /api/jobs/{id}`의
  `queue_position` 필드): 대기 중인 Job이면 상태줄에 "대기열: N번째"로 표시합니다.
- **시트작업 행 순서 정렬**: 완전한 드래그 앤 드롭 대신 각 행에 ▲▼ 버튼을 넣어 위/아래로 한
  칸씩 옮기는 방식으로 구현했습니다(드래그보다 구현이 단순하고 오동작 위험이 적음).
- **실행 전 서버측 dry-run 검증**(`preview.dry_run_validate()`, `POST /api/validate/dry-run`):
  "작업 시작"/"테스트" 클릭 시 자동으로 실행되어, 시트명이 원본·양식 양쪽에 모두 있는지, 범위
  문법(`A1:D500`, `A2` 형식)이 올바른지 확인합니다. 문제가 있으면 목록을 보여주고 "그래도
  계속할까요?"로 물어 강행할 수도 있게 했습니다(오탐 가능성을 고려해 완전히 막지는 않음).
- **작업 시작 전 "열려 있는 Excel 감지" 경고**(`job_manager.count_excel_processes()`,
  `GET /api/system/excel-status`): 종료하지 않고 개수만 세는 읽기 전용 버전. Excel이 떠 있으면
  작업 시작 전 확인 팝업을 띄웁니다.
- **업로드 파일 서버측 캐싱**(`app/file_cache.py`, `POST /api/preview/upload`): 파일을 한 번
  업로드하면 토큰을 내려주고(최대 30분/8개 보관하는 메모리 캐시), 이후 범위 선택 뷰어·자동
  추출·사전 검증·dry-run 요청은 파일을 재첨부하지 않고 토큰만 보냅니다. 원본/양식 파일을
  선택하는 즉시 백그라운드로 캐시에 올려두고, 이후 모든 미리보기 요청이 이 토큰을 재사용하도록
  프론트엔드를 바꿨습니다. 실제 Job 실행용 파일 저장(디스크, 영구)과는 별개의 캐시입니다.
- **실행 로그 다운로드/복사**: "실행 로그" 카드에 "로그 복사"(클립보드), "로그 다운로드"(.txt)
  버튼을 추가했습니다.
- **결과 파일 개별 다운로드**(`job_manager.list_result_files()`, `GET /api/jobs/{id}/results`,
  `GET /api/jobs/{id}/result/{filename}`): Job 히스토리의 "파일별 보기"에서 개별 xlsx를 바로
  다운로드할 수 있습니다. 경로 조작 방지를 위해 파일명은 `Path(...).name`으로 정리하고 실제
  해당 Job의 결과 폴더 안에 있는지 재확인합니다.
- **라이트 테마 다듬기**: 사용자가 아직 명시적으로 테마를 고른 적 없으면(저장된 값이 없으면)
  `prefers-color-scheme: light` 미디어쿼리로 OS 설정을 자동 반영하도록 했고, 테마 토글 버튼의
  라벨도 이 경우 실제 렌더링되는 색을 정확히 반영하도록 `getCurrentTheme()`을 보정했습니다.
  `--primary-disabled`의 라이트 값(`#f0e2a8`)도 추가했습니다.

## 4. 남은 백로그

- **수식 위험 함수 경고**: `INDEX`/`OFFSET` 같은 암시적 교차 위험 함수를 감지하려면 셀의 원본
  수식 문자열을 읽어야 하는데, 현재 미리보기에 쓰는 `python-calamine`은 계산된 값만 제공하고
  수식 자체를 읽는 API가 없습니다(`CalamineSheet`/`CalamineWorkbook`에 formula 관련 메서드
  없음, 직접 확인함). 이 기능을 구현하려면 (a) 이 검사만을 위해 openpyxl로 다시 열어서 수식을
  읽거나(느려짐, 특히 대용량 파일에서 이번에 calamine으로 바꾼 이유와 정면으로 배치),
  (b) xlsx 내부 XML을 직접 파싱해서 수식 문자열만 뽑아내거나 하는 추가 작업이 필요합니다.
  당장은 설명서의 "⚠ 시작 전 주의사항" 텍스트 안내로 대체합니다.
